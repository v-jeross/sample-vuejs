spec:
  name: sample-vuejs
  services:
  - build_command: yarn build
    environment_slug: node-js
    git:
      branch: main
      repo_clone_url: https://github.com/digitalocean/sample-vuejs.git
    name: sample-vuejs
  databases:
  - cluster_name: dev-db-name
    db_name: secondDb
    db_user: doadmin
    engine: PG
    name: dev-db-name
    production: false
    version: "8"
